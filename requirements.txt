# Core dependencies
typer==0.12.3
pydantic==2.5.0
pydantic-settings==2.1.0

# Database drivers
asyncpg==0.29.0          # PostgreSQL async driver (Supabase)
motor==3.3.2             # MongoDB async driver (MongoDB Atlas)
cassandra-driver==3.29.0 # Cassandra driver (AstraDB)
neo4j==5.15.0            # Neo4j driver (AuraDB)
redis==5.0.1             # Redis async driver (Redis Cloud)

# Utilities
tenacity==8.2.3          # Retry logic with exponential backoff
structlog==23.2.0        # Structured logging
python-dotenv==1.0.0     # Environment variables

# Development
pytest==7.4.3
pytest-asyncio==0.21.1